# CrapMedia brand assets

## Primary icon (poop + headphones)

**Source:** `poop.png`

Sync to both apps:

```bash
pnpm sync:brand
```

Copies into `apps/player/public/` and `apps/web/public/` as `icon.png`, `favicon.png`, and PWA `icon-512.png`.

## Other files

| File | Purpose |
|------|---------|
| `visualizer.gif` | Legacy driving visualizer reference |
| `favicon.png` / `favicon-site.png` | Old StackCP export — **not** used (wrong icon) |
