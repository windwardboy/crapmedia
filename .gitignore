# Dependencies
node_modules/
.pnpm-store/

# Build / deploy
.next/
out/
dist/
.turbo/
.vercel/
*.tsbuildinfo

# Environment & secrets
.env
.env.*
!.env.example

# OS & editors
.DS_Store
Thumbs.db
*.log

# Media (not stored in repo — users connect remote libraries)
*.mp3
*.flac
*.m4a
*.wav

# Archives & old backups
archive/
*.zip

# Legacy reference only — do not deploy as-is
legacy/music-player/music/
